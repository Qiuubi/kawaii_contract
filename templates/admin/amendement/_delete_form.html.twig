<form method="post" action="{{ path('amendement_del', {'id': amendement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ amendement.id) }}">
	<button class="btn">Delete</button>
</form>
