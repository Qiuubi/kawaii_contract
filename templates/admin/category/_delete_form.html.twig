<form method="post" action="{{ path('category_del', {'id': category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{s{ csrf_token('delete' ~ category.id) }}">
    <button class="btn">Delete</button>
</form>
