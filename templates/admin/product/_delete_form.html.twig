<form method="post" action="{{ path('product_del', {'id': product.id}) }}" onsubmit="return confirm('Êtes vous sûr de supprimer ce produit ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
	<button class="button-submit dashboard">Supprimer</button>
</form>
